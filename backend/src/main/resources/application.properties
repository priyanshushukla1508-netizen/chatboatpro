# Server Configuration
server.port=8080

# Database Configuration - H2 In-Memory (for local development)
# spring.datasource.url=jdbc:h2:mem:chatbotdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
# spring.datasource.driver-class-name=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=
# spring.datasource.hikari.connection-timeout=30000
# spring.datasource.hikari.maximum-pool-size=5
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
# spring.jpa.hibernate.ddl-auto=create-drop
# spring.jpa.show-sql=false
# spring.h2.console.enabled=true
# spring.h2.console.path=/h2-console

# Supabase PostgreSQL (production - Using Pooler on Port 6543 for IPv4 compatibility)
spring.datasource.url=${DB_URL:jdbc:postgresql://db.fltmfuntfpzlkwqfljus.supabase.co:6543/postgres?prepareThreshold=0}
spring.datasource.username=${DB_USER:postgres.fltmfuntfpzlkwqfljus}
spring.datasource.password=${DB_PASS:riyajaan@Pk}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000

# Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Spring Security / JWT
jwt.secret=${JWT_SECRET:NyijgiIRclNPuBTZ+eVB2ozKfFO318HtYX8AHRMKkN8f6qAiKzlss8qPpBLS2UmXdS+pjs14jSyEGAd+wf0arQ==}
jwt.expiration=86400000

# AI Configuration (Groq / Ollama)
groq.api.key=${GROQ_API_KEY}
groq.model=llama3-8b-8192
ollama.base.url=${OLLAMA_URL:http://localhost:11434}

# Crawler Configuration (Now using JSoup in-memory)

# Mail Configuration (optional - won't crash on startup if SMTP not available)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USER:noreply@chatbotpro.in}
spring.mail.password=${MAIL_PASS:placeholder}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
